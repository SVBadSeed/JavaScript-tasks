// Что выведет этот скрипт?

let name = "Ilya"

alert(`hello ${1}`) // выведет hello 1

alert(`hello ${"name"}`) // выведет hello name тк name вставлен в ${} как строка.

alert(`hello ${name}`) // выведет hello Ilya тк name  вставлен как переменная со значением Ilya